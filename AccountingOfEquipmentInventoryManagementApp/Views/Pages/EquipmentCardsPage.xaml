<Page x:Class="AccountingOfEquipmentInventoryManagementApp.Views.Pages.EquipmentCardsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:AccountingOfEquipmentInventoryManagementApp.Helpers"
      Title="Оборудование">

    <Page.Resources>
        <local:ByteArrayToImageConverter x:Key="ByteArrayToImageConverter" />
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <ItemsControl ItemsSource="{Binding EquipmentList}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel IsItemsHost="True" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Gray" BorderThickness="1" Background="White" Padding="10" Margin="10" CornerRadius="8">
                        <StackPanel Width="200">
                            <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16" Margin="0,0,0,5"/>

                            <TextBlock Text="Статус:" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding Status}" Margin="0,0,0,5"/>

                            <TextBlock Text="Серийный номер:" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding SerialNumber}" Margin="0,0,0,5"/>

                            <TextBlock Text="Категория:" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding Category.Name}" Margin="0,0,0,5"/>

                            <TextBlock Text="Местоположение:" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding Location}" Margin="0,0,0,10"/>

                            <Image Height="100" Stretch="Uniform"
                                   Source="{Binding Image, Converter={StaticResource ByteArrayToImageConverter}}" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</Page>