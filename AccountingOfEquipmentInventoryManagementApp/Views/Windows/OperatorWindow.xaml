<Window x:Class="AccountingOfEquipmentInventoryManagementApp.Views.Windows.OperatorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:AccountingOfEquipmentInventoryManagementApp.ViewModels"
        Title="Оператор" Height="600" Width="1000"
        WindowStyle="None" AllowsTransparency="True"  Background="White">

    <Window.DataContext>
        <local:OperatorViewModel/>
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <!-- Верхняя панель -->
            <RowDefinition Height="*"/>
            <!-- Всё остальное -->
        </Grid.RowDefinitions>

        <!-- Панель кнопок управления окном -->
        <Grid  Height="30" VerticalAlignment="Top" MouseLeftButtonDown="CustomTitleBar_MouseLeftButtonDown">
            <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Center"
                    Margin="875,0,0,0"
                    Grid.Row="0"
                    Panel.ZIndex="999" Height="40">
                <Button Width="30" Height="30" Content="−"
                    Command="{Binding WindowControls.MinimizeCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                    ToolTip="Свернуть"/>
                <Button Width="30" Height="30" Content="☐"
                    Command="{Binding WindowControls.MaximizeCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                    ToolTip="Развернуть"/>
                <Button Width="30" Height="30" Content="✖"
                    Command="{Binding WindowControls.CloseCommand}"
                    CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                    ToolTip="Закрыть"/>
            </StackPanel>
        </Grid>

        <!-- Основной контент -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Панель управления -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,10">
                <Label Content="Поиск:" VerticalAlignment="Center" Margin="5"/>
                <TextBox Width="200" Margin="5" Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}"/>
                <Label Content="Категория:" VerticalAlignment="Center" Margin="5"/>
                <ComboBox Width="150" Margin="5"
                          ItemsSource="{Binding Categories}"
                          SelectedItem="{Binding SelectedCategoryFilter}"
                          DisplayMemberPath="Name"/>
                <Button Content="Найти" Width="100" Margin="5" Command="{Binding SearchCommand}"/>
                <Button Content="Сбросить" Width="100" Margin="5" Command="{Binding ClearFilterCommand}"/>
                <Button Content="Обновить данные" Width="140" Margin="5" Command="{Binding RefreshCommand}"/>
                <Button Content="Экспорт" Command="{Binding ExportCommand}" Width="100" Margin="0,5,5,5"/>
            </StackPanel>

            <!-- Таблица -->
            <DataGrid Grid.Row="1" AutoGenerateColumns="False"
          IsReadOnly="True" SelectionMode="Single"
          Margin="0,0,0,10"
          ItemsSource="{Binding InventoryList}"
          SelectedItem="{Binding SelectedEquipment, Mode=TwoWay}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                    <DataGridTextColumn Header="Наименование" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="Серийный номер" Binding="{Binding SerialNumber}" Width="150"/>
                    <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="100"/>
                    <DataGridTextColumn Header="Категория" Binding="{Binding Category.Name}" Width="150"/>
                    <DataGridTextColumn Header="Дата покупки" Binding="{Binding PurchaseDate, StringFormat=\{0:dd.MM.yyyy\}}" Width="150"/>
                    <DataGridTextColumn Header="Местоположение" Binding="{Binding Location}" Width="150"/>
                    <DataGridTemplateColumn Header="Изображение" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Просмотр"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Command="{Binding DataContext.ViewImageCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                            CommandParameter="{Binding Image}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>


            <!-- Статус-бар -->
            <StatusBar Grid.Row="2">
                <StatusBarItem>
                    <TextBlock Text="{Binding Status}"/>
                </StatusBarItem>
            </StatusBar>
        </Grid>
    </Grid>
</Window>